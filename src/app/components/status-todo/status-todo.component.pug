mixin status-todo(data)
	- data = data || {}
	- data.class = data.class || ''
	- data.content = data.content || 'Some content here'

	.status-todo(class=data.class)&attributes(attributes)
		if block
			block
		else
			!= data.content